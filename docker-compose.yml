services:
  video-auto-composition:
    build: .
    container_name: video-auto-composition
    ports:
      - "5000:5000"
    volumes:
      - ./assets/:/app/assets/
      - ./app.py:/app/app.py
    environment:
      - FLASK_APP=app.py
      - FLASK_ENV=development
      - FLASK_DEBUG=1
